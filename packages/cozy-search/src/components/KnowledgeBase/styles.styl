.intentHost
    width 100%
    height 70vh

    // cozy-interapp injects a bare <iframe> with no styling of its own
    // (browser default is 150px tall) — make it fill the host
    iframe
        width 100%
        height 100%
        border none
